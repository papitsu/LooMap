<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loomap">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.loomap.VisitActivity"></activity>
        <activity android:name="com.example.loomap.EditToiletActivity" />
        <activity android:name="com.example.loomap.EditVisitActivity" />
        <activity
            android:name="com.example.loomap.MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.loomap.AddToiletActivity" />
        <activity android:name="com.example.loomap.AddVisitActivity" />
        <activity android:name="com.example.loomap.SettingsActivity" />
        <activity android:name="com.example.loomap.StatsActivity" />
        <activity android:name="com.example.loomap.ToiletActivity" />
        <activity android:name="com.example.loomap.ToiletsActivity" />
        <activity android:name="com.example.loomap.VisitsActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBVAivux5rjs1LQ0we82ntiAITmMJqkViM" />
    </application>

</manifest>